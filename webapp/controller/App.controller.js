sap.ui.define(["be/rpan/epm/suppliers/base/BaseController"], function(
	BaseController
) {
	"use strict";
	const AppController = BaseController.extend(
		"be.rpan.epm.suppliers.controller.App" /** @lends be.rpan.epm.suppliers.controller.App.prototype */,
		{}
	);

	return AppController;
});
